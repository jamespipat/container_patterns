# warehouse-leaf-py has no third-party dependencies: it is a stdlib-only
# HTTP service (http.server + json). This file is intentionally empty of
# packages so the image stays minimal and reproducible.
